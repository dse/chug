# chug

Chug is a crappy static website generator based on Gulp.  It uses the
following:

-   Sass, the enhancement language for CSS.

-   Nunjucks, the HTML template system.

-   BrowserSync, the web server.

-   Gulp, the task runner and file change watcher.
